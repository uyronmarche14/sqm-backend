Manual-only seed assets.

These files are intentionally not executed by any default backend command.

Use cases:

- historical form inventory backfills
- legacy/demo data inspection
- one-off operator review

Do not wire this directory into runtime startup, safe bootstrap, or default seeding.
