Manual-only migrations.

These scripts are intentionally excluded from `db:migrate:safe`.

Typical contents:

- form inventory additions
- route-alignment updates
- operator-managed metadata reconciliation

Run only after explicit review.
